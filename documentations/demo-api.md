import { afterEach, describe, expect, it } from "vitest";

import { clearRagStore, persistDocument, searchDocuments } from "@/lib/rag/store";
import type { DocumentIntakeResult } from "@/lib/documents/types";

afterEach(async () => {
  await clearRagStore("vitest-project");
});

describe("RAG store facade", () => {
  it("persists chunks and retrieves relevant project context", async () => {
    const intake: DocumentIntakeResult = {
      documentId: "doc-vitest",
      fileName: "vitest.txt",
      mimeType: "text/plain",
      size: 120,
      textPreview: "The backend shall retrieve document chunks for SRS answers.",
      extractedTextLength: 58,
      chunks: [
        {
          id: "doc-vitest-chunk-001",
          index: 0,
          text: "The backend shall retrieve document chunks for SRS answers.",
          startOffset: 0,
          endOffset: 58,
        },
      ],
      findings: [],
    };

    const persisted = await persistDocument({ projectId: "vitest-project", intake });
    const results = await searchDocuments("vitest-project", "retrieve chunks for SRS", 3);

    expect(persisted.chunks).toHaveLength(1);
    expect(["local", "supabase"]).toContain(persisted.storeProvider);
    expect(results).toHaveLength(1);
    expect(results[0].chunkId).toBe("doc-vitest-chunk-001");
    expect(results[0].score).toBeGreaterThan(0);
  });
});


## Supabase pgvector Setup

Run this SQL migration in Supabase before enabling Supabase-backed RAG:

```text
supabase/migrations/001_rag_pgvector.sql
```

Set these environment variables in `.env.local` or Vercel:

```env
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DOCUMENTS_TABLE=documents
SUPABASE_CHUNKS_TABLE=document_chunks
SUPABASE_MATCH_FUNCTION=match_document_chunks
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
```

When these Supabase variables are present, document upload and retrieval use Supabase pgvector. If Supabase is not configured, the backend uses the local `.rag-store/` development store with OpenAI-generated embeddings. If Supabase is configured but fails, the API surfaces the storage error instead of silently switching stores.



## Guardrails And Error Handling

The API includes a shared guardrail layer for demo-safe request validation:

- JSON API requests are capped at 256 KB before parsing.
- Multipart document uploads are capped at 10 MB at the API boundary, with an 8 MB document-processing limit.
- `projectId` values must be short, URL-safe identifiers containing only letters, numbers, hyphens, and underscores.
- Agent messages, developer intent, existing requirements, document text, and retrieval queries have explicit maximum lengths.
- Validation errors return HTTP `400` with a stable `code` and field-level `issues` when available.
- Guardrail violations return HTTP `400` with a stable code such as `request_too_large`.
- Provider/configuration failures return HTTP `503` and do not produce deterministic fallback content.
- Unexpected server failures return HTTP `500` with a generic safe message while details are logged server-side.

## AI Provider, Tool Calling, And Response Policy

`POST /api/agent` is now backed by a server-side tool-calling requirement agent and requires OpenAI. The agent first runs a controlled tool step with AI SDK `generateText`, where the model can call `retrieve_project_context` to inspect uploaded project documents through RAG. The tool output is then passed into a final `generateObject` step that returns the same validated `RequirementAgentResponse` contract used by the frontend.

Current server-side agent tools:

- `retrieve_project_context`: retrieves relevant uploaded document chunks for the current `projectId` and user request, using Supabase pgvector when configured or the local development RAG store when Supabase is not configured. The server rejects retrieval for any project ID other than the request project.
- `inspect_interview_memory`: inspects the current request, stored answers, answered topics, and transcript context provided by the frontend.
- `assess_requirement_readiness`: checks whether the available context is ready for the next question, diagram generation, or SRS generation.
- `validate_srs_evidence`: checks whether the available evidence is sufficient for a developer-ready SRS without inventing missing facts.

Client-facing agent responses, questionnaire questions, answer options, Mermaid diagrams, and SRS content are still generated by the configured OpenAI model. Static backend code validates schemas, executes approved tools, parses documents, stores/retrieves embeddings, and generates PDF files, but it must not act as a deterministic requirement-answer generator for the agent experience.

The system must not silently replace failed LLM or tool responses with deterministic template output. If `OPENAI_API_KEY` is missing, OpenAI fails, or a configured storage provider fails, the API returns a clear error instead of fabricating content.
